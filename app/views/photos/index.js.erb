$('tbody#photos').html('<%= escape_javascript render(partial: "photo", :collection => @photos, :as => :photo) %>');

$('#paginator').html('<%= escape_javascript(paginate(@photos, :remote => true).to_s) %>');
